experiment_name: llama32_1b_baseline_no_help
seed: 42

model:
  name: unsloth/Llama-3.2-1B-Instruct
  backend: hf
  torch_dtype: bfloat16
  device_map: auto
  attn_implementation: null

generation:
  max_new_tokens: 96
  do_sample: false
  temperature: 0.0
  top_p: 1.0

prompting:
  template: no_help

steering:
  enabled: false

batching:
  batch_size: 2

dataset:
  path: data/raw/ambik/ambik_test_400.csv
  limit: 2

output:
  root_dir: outputs
